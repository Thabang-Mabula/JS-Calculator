
/**
 * @class UnitaryOperator
 * @description Class that acts as an interface for the implementation of unitary operators (i.e. operators that operate on only one operand)
 * 
 */
export default class UnitaryOperator {
    /**
     * Constructor
     */
    constructor() { }

    /**
     * Executes the operation operand
     * @param {Number} operand Operand
     * @returns {Number} Result of the calculation
     */
    execute(operand) { }
}